# 주간 운영 리뷰 — 2026-08-10

## 1. 지표 요약
- **총원 968명 · 그룹 177**(전주 08-03: 775명·124그룹, **+193명·+53그룹**). 정합성 **OK**
  (`statsTotalMatches=true`, 중복 0, `node tools/build.js --check` / `node tools/ops-metrics.js --check`
  모두 errors/computeMismatch/anchorFails/duplicates 전부 빈 배열로 클린).
- **카테고리 노출**: K-idol(894)·J-idol(36) 기존 노출에 이어 **US-actor(30)가 이번 주
  CAT_MIN(30) 문턱을 넘어 신규 노출**(전주 4명 → 30명, 일요일 Task D 확장. 커밋
  `ce2fd17`, 08-09). J-idol 이어 **두 번째 비K 카테고리 개방**. C-actor(3)·Etc(5)는
  여전히 문턱 미달로 숨김(개별 인물 수집이라 느린 성장, 문제 아님).
- **소속사 분포**: 기타 622(+156) · SM 80 · HYBE 78(+5) · JYP 74 · YG 40 · 노기자카46 30 ·
  미국 배우 30(신규 노출과 함께) · LAPONE 6 · 배우 5 · 중국 배우 3.
- **subAgency 세분화(Phase 1)**: 커버리지 **51% → 70%**(238→436/622명 라벨 부여).
  `AGENCY_MIN=30` 승격 후보(`promotable`)는 전주와 동일 **2개**(스타쉽엔터테인먼트 45·
  MODHAUS 54, 전주 32/51에서 증가). 다만 15~29명 구간에 후보 8개가 몰려있음(FNC
  엔터테인먼트 25·울림엔터테인먼트 24·웨이크원 20·큐브엔터테인먼트 20·
  아이에스티엔터테인먼트 19·KQ엔터테인먼트 18·인코드엔터테인먼트 18·C9엔터테인먼트 15) —
  다음 1~2주 내 promotable 확대 가능성.
- **제보 백로그**: 추가 대기 **251**(전주 454 대비 **-45%**, 고유 그룹 209 — 전주 319 대비
  -34%) · 오류 대기 **0**(95건 전량 `inbox-done` 처리 확인) · 처리 완료 1,603(전주 1,247,
  +356).
- **i18n**: 15/15 청크 **100%** 유지(KO/EN/JA/ZH 4개 언어 전 화면 지원).
- **flushCadence**: 최근 7일 flush 47건, 평균 배치 1.1, cron 지연 이벤트 0건 —
  `assessment: "정상(최근 48h 누적-지연 없음)"`. 워커 큐→cron 파이프라인 건강.

## 2. 오류 처리 내역
- `error-inbox.json` 미처리(pending) 제보 **0건** — 95건 전부 이미 `inbox-done.json`에
  반영되어 재처리 대상 없음. 이번 주 `idols.json` 데이터 오류 자동 수정 **없음**(확증 대상
  자체가 없었음).
- 스팟체크 1건(QA 렌즈, 리뷰 중 발견한 "이상 후보"는 아니고 지난주 수정분 재확인 목적):
  지난주(`eeda9b0`) 수정된 **ILLIT 민주 생일 2004-05-11**을 WebSearch로 교차검증 →
  위키백과·나무위키 프로필 정보와 일치, 추가 수정 불필요.
  Sources: [민주 (2004년생 가수) - 위키백과](https://ko.wikipedia.org/wiki/%EB%AF%BC%EC%A3%BC_(2004%EB%85%84%EC%83%9D_%EA%B0%80%EC%88%98)),
  [민주(ILLIT) - 나무위키](https://namu.wiki/w/%EB%AF%BC%EC%A3%BC(ILLIT))
- 오신고로 재분류한 건 없음(대기 자체가 0건).

## 3. 페르소나 리뷰

**QA(4년차).** 정합성 100%, 오류 백로그 0, 회귀 없음. 08-07 배포된 신규 기능
(`4d1edba`, 삼각관계 생년월일 직접입력)에 대해 이번 리뷰에서 직접 헤드리스 스모크를
실행(모바일 390px) — `openTri()`→입력→`runTri()` 정상 렌더, JS 에러 0, 모달 가로
오버플로 없음(`scrollWidth==clientWidth==390`) 확인. 해당 커밋 메시지엔 스모크 실행 기록이
없었는데, 앞으로 UI 기능 커밋은 데이터 커밋처럼 스모크 로그를 메시지에 남기는 관행을
권장(§4 참고).

**개발자(4년차).** flushCadence 정상(cron 지연 0, 평균 배치 1.1) — 워커 큐→cron 파이프라인
이상 없음. 이번 주 총원 +193명(+25%)은 최근 중 가장 빠른 증가지만, 일요일 Task D
(US-actor 30 달성, +26) + 평일 Task B 다회 소진 + 화·목 subAgency/i18n 슬롯이 겹친
결과로 설명되고 정합성 100% 유지 — 시스템 부하나 파이프라인 이상 아님. subAgency
promotable이 이번 주 코드 변화 없이 정체(2개)지만 15~29 구간 후보가 늘고 있어, Phase 2
(`index.html` `renderAgencyButtons`) 착수 타이밍을 다음 1~2회 리뷰에서 재모니터링할 것을
제안.

**기획자(4년차).** 최초 기획의도(사전지식 없이 사주 특성만으로 오락적 최애 추천) 유지
확인. US-actor 신규 노출로 J-idol에 이은 두 번째 비K 카테고리 개방 — 코드 변경 없이
데이터만으로 자동 달성, 기획의도 그대로 적용됨. 신규 기능 중 "최애끼리" 궁합에서
"사귄다면/커플/천생연분" 등 로맨틱 프레이밍을 삭제하고 "케미 등급"으로 대체한 변경(`4d1edba`)은
**동의** — 실존 아이돌 간 로맨틱 셰이핑을 지양해 브랜드 리스크를 낮추면서 오락성(케미
서술)은 유지, 기획의도와 상충 없음. 삼각관계 모달의 생일 직접입력은 메인 매칭 없이 바로
진단 가능하게 해 진입 마찰을 줄이는 방향으로 기획의도(즉시성·재미)에 부합.

**디자이너(3년차).** 삼각관계 모달 모바일(390px) 스모크 통과, 오버플로 없음(QA와 공동
확인). 이번 주 신규 기능은 모두 기존 모달 패턴을 재사용해 정보 위계 변화 없음. 관찰(제안
아님): subAgency 라벨이 35개까지 늘어났으나 Phase 1(데이터만)이라 아직 UI 미노출 — Phase 2
논의가 재개될 경우 상단 버튼 수 증가로 인한 모바일 가로 스크롤/wrap 여부를 반드시 스모크에
포함해야 함(PROPOSALS.md P2 기존 리스크 항목 그대로 유효, 재확인만).

**팀장(8년차) 종합·판정.** 이번 주는 **안정적 확장 주**로 판정한다 — 정합성 100%, 오류
백로그 0, 추가 백로그 -45%(고유 그룹 -34%) 추세 지속, 신규 카테고리(US-actor) 무사고
노출, 신규 UX 2건(삼각관계 직접입력·최애끼리 프레이밍 정리) 모두 스모크 통과. **액션 아이템
없음** — 모든 지표가 정상 범주. 유일한 판단 포인트는 P2(소속사 세분화) Phase 2 착수
타이밍인데, 이번 주 promotable이 전주와 동일(2개)이라 아직 "커버리지가 더 쌓인 뒤"라는
기존 보류 조건을 재확정한다. **권고:** promotable이 4개 이상으로 늘어나는 시점(다음 1~2주
내 가능성 높음)에 Phase 2 착수 여부를 다시 사용자에게 묻는다. 그 전까지는 Task A가 계속
데이터만 채운다(무위험).

## 4. 개선 제안 (승인 대기)
- 상세 스펙은 [`../ops/PROPOSALS.md`](../ops/PROPOSALS.md)에 항목으로 관리한다. 이번
  주는 새 제안 없이 **P2 항목의 진행 현황만 갱신**했다(coverage 70%, promotable 2개 유지,
  15~29 구간 후보 8개 — Phase 2 착수는 여전히 사용자 승인 대기, 팀장은 promotable 4개
  이상 시점에 재문의 권고).
- 운영 관행 제안(코드/데이터 변경 아님): UI 기능 커밋 시 헤드리스 스모크 실행 여부를
  커밋 메시지에 명시하는 관행 — 리스크 낮음, 다음 세션부터 바로 적용 가능한 관행이라
  별도 PROPOSALS 항목 없이 여기 기록만 남김.

## 5. 신규 데이터·기능 아이디어
- 카테고리가 CAT_MIN(30) 문턱을 새로 넘길 때(이번 주 US-actor처럼) 결과 화면에 "새 카테고리
  열림" 토스트를 1회성으로 노출하면, 이미 있는 신규 그룹 토스트 UX 패턴을 재사용해 소규모
  공수로 발견성을 높일 수 있음(코드 변경 필요 — 제안일 뿐, 승인 전 미착수).
- subAgency 라벨이 늘어난 만큼, 검색창에 소속사 하위 라벨(예: "스타쉽")로도 그룹을 찾을 수
  있게 하는 아이디어(현재는 그룹명/멤버명 검색만) — Phase 2(상단 버튼 승격)와 별개로 검토
  가능한 저리스크 검색 확장안. 구체 스펙 미작성, 아이디어 단계.

## 6. 다음 주 액션
1. 백로그 소진 지속(요일 고정 스케줄 유지, 현재 추세 정상).
2. subAgency `promotable` 모니터링 — 4개 이상 도달 시 Phase 2(P2) 착수 여부 사용자에게
   재문의.
3. UI 기능 커밋 시 헤드리스 스모크 실행 기록을 커밋 메시지에 남기는 관행 적용.
4. 확증된 데이터 오류가 없는 한 다음 정기 리뷰(2026-08-17)까지 특이 대응 불필요.

## 부록: Notion 미러링
**노션 미러링 생략(커넥터 없음)** — 이번 세션에 Notion MCP 툴이 연결되지 않아 시도하지
않음(실패 아님, best-effort 규칙). 저장소 `docs/reports/`가 정본이다.
