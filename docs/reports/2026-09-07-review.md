# 주간 운영 리뷰 — 2026-09-07

## 1. 지표 요약
- **총원 1,573명**(전주 1,438 대비 **+135**) · **그룹 310**(+24) · 정합성 100%
  (`build.js --check` 클린, `stats.json.total`==`idols.json` 수, `name|group` 중복 0).
- **카테고리 노출 5종 유지**: K-idol(1,335) · J-idol(112) · C-actor(38) · K-actor(35) ·
  US-actor(31). 구성 변화 없이 순수 데이터 증가(J-idol이 +33로 가장 크게 성장 — 일요일
  Task D 슬롯 누적). Etc(22, 전주 7)는 여전히 `CAT_MIN=30` 미달로 숨김.
- **소속사 분포**: 기타 1,044 · SM 86 · HYBE 92 · JYP 80 · YG 40 · 그 외(중국 배우·미국
  배우·노기자카46·LAPONE 등). `기타` 중 **945명(91%, 전주 86%) subAgency 부여** — 커버리지
  +5pp 반등(전주는 `기타` 모수 급증으로 일시 하락, 이번 주는 라벨링(+114)이 모수 증가(+73)를
  앞질러 정상 회복). `AGENCY_MIN=30` `promotable`이 **6→7개**로 확대: 스타쉽엔터테인먼트 60 ·
  MODHAUS 56 · FNC엔터테인먼트 41 · **웨이크원 33(신규)** · 스윙엔터테인먼트 31 ·
  큐브엔터테인먼트 30 · 아이에스티엔터테인먼트 30 — **Phase 2 착수 여부 사용자 응답 대기
  4주째**([`PROPOSALS.md`](../ops/PROPOSALS.md) P2).
- **제보 백로그**: 추가 대기 **7**(전주 26 대비 **-73%**, 고유 그룹 7·전주 25 대비 **-72%**,
  6주 연속 감소) · 오류 대기 **0**(100건 전량 처리 확인, 신규 오류 제보 없음) · 처리 완료 2,006
  (전주 1,970, +36).
- **i18n**: **16/16 청크(100%)** 유지. 4개 언어(KO/EN/JA/ZH) 전 화면 지원.
- **flushCadence**: 최근 7일 `chore(inbox): flush` 19건, 최대 배치 2·평균 1.1, cron-delay
  경보 0건 — **정상**(누적-지연 없음, quiet gap 18건은 대부분 조용한 시간대).
- **콘텐츠 사용 현황(`contentUsage`)**: `available=true`. 최근 7일 순사용자 합
  **2명**(전주 0명에서 소폭 회복), 누적 17명. 랭킹 — 사주궁합(match) 1/7(50%) · 결과
  공유(share) 1/3(50%) · 최애끼리 궁합(pair) 0/3 · 최애 프로필(profile) 0/2 · 지옥의
  삼각관계(tri) 0/1 · 제보 열기(suggest) 0/1 · **케이팝 오타쿠 사주(otaku) 0/0(여전히
  미계측)**. **표본 극소(7일 합 2명)라 인기/저조 판단은 보류** — §3 개발자 관찰 참조.

## 2. 오류 처리 내역
- `error-inbox.json` 100건 전량 `inbox-done.json` 반영 완료 확인, 신규 오류 제보 0건 —
  **이번 주 WebSearch 교차검증 대상 없음, `idols.json` 데이터 오류 자동 수정 0건.**
- "이상 후보"(카테고리 문턱 미달·특정 그룹 이상)도 이번 주 신규 발견 없음(Etc는 개별 인물
  수집 특성상 정상적으로 느린 성장 — 기존 관찰과 동일).

## 3. 페르소나 리뷰
- **QA**: 정합성 100%(`statsTotalMatches`, 중복 0), 오류 백로그 0(재확인 완료). 제보 백로그
  6주 연속 최대 수준 감소(26→7) — 요일 고정 SOP가 안정적으로 소진 중, 회귀 없음.
- **개발자**: **핵심 관찰 — `tools/usage.json` flush가 08-24 이후 13일간 완전히 멈췄다가
  09-06 새벽(02:50 KST)에 재개됐다.** 재개 직후 패턴을 전수 확인한 결과:
  ① 이벤트 목록이 여전히 **6종**(`otaku` 미포함) — **P7 미해결**(배포본이 여전히 08-19 이전
  구버전). ② 재개 후 하루 커밋이 다시 **80~144건**으로 치솟았고 diff는 전부 `updatedAt`
  한 줄뿐 — **P6 미해결**(no-op 억제 로직 미반영). **결론: 08-31 리뷰가 예상한 "재배포 후에만
  구분 가능"이 실현됐다 — 재개는 "무방문 지속"이 아니라 계측 경로 자체(비콘 키 TTL 등)가
  되살아난 것이지만, 배포본 자체는 재배포되지 않았다(P6/P7 코드 미반영 그대로).** 즉 워커가
  껐다 켜졌거나 KV 상태가 리셋됐을 가능성은 있으나, **사용자가 요청한 코드 수정판 재배포는
  아직 이뤄지지 않았다.** flushCadence(제보 큐)는 19건/7일·정상, 빌드 성능 이슈 없음.
- **기획자**: 최초 기획의도(사주 특성만으로 오락적 최애 추천) 유지 확인, 이번 주 데이터·UI
  변경 없음. `contentUsage` 최근 7일 순사용자 합이 2명으로 여전히 극소 표본이라 **인기/저조
  콘텐츠 판단은 이번 주도 보류** — 08-19 출시한 오타쿠 사주는 3주째 실사용 여부를 전혀 볼 수
  없는 상태(P7이 막고 있음). 재배포가 늦어질수록 신규 콘텐츠 성과 판단이 계속 미뤄진다.
- **디자이너**: 표본 부족으로 저조 콘텐츠의 진입 동선·설명 점검은 이번 주도 수행 불가(기획자와
  동일 사유). UI 변경이 없어 별도 회귀 관찰 없음.
- **팀장 종합·판정**: 데이터 파이프라인은 이번 주도 건강하다(정합성 100%·백로그 6주 연속
  최대 감소·flushCadence 정상). **유일한 상시 리스크는 여전히 워커 재배포 건**이며, 이번 주
  새로 확인된 사실(재개는 됐지만 구버전 그대로)은 "재배포를 더 미룰 이유가 없다"는 근거를
  강화한다 — 콘텐츠 사용 계측과 신규 모드(오타쿠 사주) 성과 판단이 3주 넘게 막혀 있다.
  **명확한 권고: P6+P7을 묶어 재배포를 우선순위 최상단으로 승인 요청**(코드는 이미 저장소에
  준비돼 있고 변경 리스크 없음). P2(소속사 Phase 2)는 조건이 계속 무르익고 있으나(promotable
  7개) 재촉하지 않고 대기 유지.

## 4. 개선 제안 (승인 대기)
- **P6·P7 (재상정, 이번이 3주째)**: 워커 usage flush no-op 커밋 차단(3줄) + `otaku` 계측
  누락 해소 — **재배포 1회로 둘 다 해결**. 이번 주 새 근거: flush가 13일 만에 재개됐지만
  배포본은 그대로(구버전 증상 그대로 재현). 상세: [`../ops/PROPOSALS.md`](../ops/PROPOSALS.md) P6·P7.
- **P2 (응답 대기 4주째)**: `기타` 소속사 Phase 2(상단 버튼 승격) 착수 여부. `promotable`
  6→7개(웨이크원 신규). 상세: PROPOSALS.md P2.
- **P8 (결정 대기)**: 동일 인물 중복 노출 정책(표시 단계 제거 권고, (c)안). 이번 주 별도
  재검사는 수행하지 않음(마지막 확인값 24건, 08-31 기준 유지) — 신규 대규모 재검증은 다음
  전수 조사 주기에. 상세: PROPOSALS.md P8.
- **P9 (제안, 낮은 우선순위)**: 카테고리 한글 라벨 이중 관리 제거, `currentList()` 가드 추가.
  상세: PROPOSALS.md P9.

## 5. 신규 데이터·기능 아이디어
- 이번 주 신규 아이디어 없음(데이터 파이프라인 안정 운영에 집중).

## 6. 다음 주 액션
- P6/P7 재배포 승인 대기 계속 추적(4주차 진입 시 재상정 문구 강화 검토).
- P2 promotable 7개 도달 상태 유지 갱신, 재촉 없이 대기.
- 워커 재배포가 실제 이뤄지면 `otaku` 이벤트 계측 정상화 여부와 `contentUsage` no-op 커밋
  소멸 여부를 다음 리뷰에서 확인.
