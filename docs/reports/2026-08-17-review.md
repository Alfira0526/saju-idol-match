# 주간 운영 리뷰 — 2026-08-17

## 1. 지표 요약
- **총원 1,137명 · 그룹 237**(전주 08-10: 968명·177그룹, **+169명·+60그룹**). 정합성 **OK**
  (`statsTotalMatches=true`, 중복 0, `node tools/build.js` / `node tools/ops-metrics.js --check`
  모두 errors/computeMismatch/anchorFails/duplicates 전부 빈 배열로 클린).
- **카테고리 노출**: K-idol(1,019)·J-idol(43)·US-actor(31)에 이어 **C-actor(37)가 이번 주
  CAT_MIN(30) 문턱을 넘어 신규 노출**(전주 3명(숨김) → 37명, 커밋 `4971845`
  "M.O.N.T/MBLAQ/ALLDAY PROJECT/OMEGA X gap-fill + expand C-actor to 37 (Task D)"). J-idol·
  US-actor에 이은 **세 번째 비K 카테고리 개방** — README STATS 블록에도 반영됨. Etc(7)만
  여전히 문턱 미달(개별 인물 수집이라 느린 성장, 문제 아님).
- **소속사 분포**: 기타 741(+119) · SM 86(+6) · HYBE 78 · JYP 74 · 중국 배우 37(신규 노출) ·
  YG 40 · 미국 배우 31 · 노기자카46 30 · XG 7 · 배우 6 · LAPONE 6 · 페포니뮤직 1.
- **subAgency 세분화(Phase 1)**: 커버리지 **70% → 79%**(436→584/741명 라벨 부여).
  `AGENCY_MIN=30` 승격 후보(`promotable`)가 전주 **2개 → 4개**로 확대
  (스타쉽엔터테인먼트 51 · **큐브엔터테인먼트 30(신규)** · **FNC엔터테인먼트 35(신규)** ·
  MODHAUS 54) — SNAPSHOT/PROPOSALS P2에 명시된 **"promotable 4개 이상 도달 시 재문의"
  트리거를 이번 주 충족**. §4 참고.
- **제보 백로그**: 추가 대기 **126**(전주 251 대비 **-50%**, 고유 그룹 112 — 전주 209 대비
  -46%) · 오류 대기 **0**(96건 전량 `inbox-done` 처리 확인, 신규 미처리 없음) · 처리 완료
  1,791(전주 1,603, +188).
- **i18n**: 15/15 청크 **100%** 유지(KO/EN/JA/ZH 4개 언어 전 화면 지원). 변동 없음.
- **flushCadence**: 최근 7일 flush 32건, 평균 배치 1.1, 중위 간격 50분, cron 지연 이벤트
  0건 — `assessment: "정상(최근 48h 누적-지연 없음. 큰 gap은 대부분 조용한 시간대·제보
  적음)"`. 워커 큐→cron 파이프라인 건강.
- **콘텐츠 사용 현황(`contentUsage`)**: `available: true`이나 **표본이 매우 희박** —
  `usage.json`이 2026-08-13(1일치)에서 **3일째 갱신되지 않음**(`updatedAt` 08-14 19:40
  그대로), 최근 7일 총 이벤트 6건(사주궁합/최애끼리 궁합/최애 프로필/삼각관계/공유/제보
  각 1명, 점유율 각 16.7%로 전부 동률). 이번 주 사이 `chore(usage)` flush 커밋이 다수
  발생했음에도 `daily` 객체에 새 날짜 키가 하나도 추가되지 않음 — **인기/저조 콘텐츠
  판단은 아직 통계적으로 무의미**(표본 6명). §3 개발자 관찰 참고.

## 2. 오류 처리 내역
- `error-inbox.json` 미처리(pending) 제보 **0건** — 96건 전부 이미 `inbox-done.json`에
  반영되어 재처리 대상 없음(전주 대비 신규 오류 제보 없음).
- 리뷰 중 발견한 별도 "이상 후보" 없음(카테고리 문턱 상태는 전부 예상 범위 —
  C-actor 신규 노출은 계획된 Task D 확장 결과이지 데이터 이상이 아님) → **WebSearch
  교차검증 대상 없음, `idols.json` 자동 수정 0건**.
- 오신고로 재분류한 건 없음(대기 자체가 0건).

## 3. 페르소나 리뷰

**QA(4년차).** 정합성 100%(`statsTotalMatches`·중복 0), 오류 백로그 0, 회귀 징후 없음.
`node tools/build.js` 실행 결과 errors/computeMismatch/anchorFails/duplicates 전부 빈
배열 확인. 이번 주는 리포트·metrics·SNAPSHOT·README만 변경(데이터/UI 무변경)이라
헤드리스 스모크는 생략(OPS_ROUTINES §6 규정과 일치). 제보 백로그(추가 126·고유 그룹
112)는 전주 대비 -50%/-46%로 요일 고정 스케줄이 안정적으로 소진 중 — 액션 불필요.

**개발자(4년차).** flushCadence 정상(cron 지연 0, 평균 배치 1.1, 중위 간격 50분) —
워커 큐→cron(제보) 파이프라인 이상 없음. 다만 **usage 파이프라인 관찰 필요**: 8/12
도입된 콘텐츠 사용 계측이 8/13 하루치(총 6건, 항목당 1명)에서 3일째 정체 — `chore(usage)`
flush 커밋은 계속 발생하는데(내용은 매번 동일) 새 날짜 버킷이 생기지 않음. 두 가지
해석 가능: ① 실제로 8/14 이후 순사용자가 0명(개인 프로젝트 초기 단계라 충분히 가능,
파이프라인 정상) ② 워커 KV→미러링 경로가 새 날짜를 못 읽고 있음(파이프라인 이상).
현재로선 구분할 근거가 없고 egress 차단으로 워커 로그 직접 확인 불가 — **판단 보류,
다음 1~2주 `updatedAt`이 계속 08-14에 멈춰 있으면 팀장 판단으로 워커 점검 요청 권고**
(지금은 코드 변경 제안 아님, 관찰 기록만).

**기획자(4년차).** 최초 기획의도(사전지식 없이 사주 특성만으로 오락적 최애 추천) 유지
확인. C-actor 신규 노출로 J-idol·US-actor에 이은 **세 번째 비K 카테고리 개방** — 코드
변경 없이 데이터만으로 자동 달성, 기획의도 그대로 적용됨(비K 확장 로드맵 사실상 완주,
남은 건 Etc뿐이나 개별 수집이라 자연 성장에 맡김). `contentUsage`로 콘텐츠 우선순위를
매기려 했으나 표본이 6명뿐이라 이번 주는 "인기/저조" 판단을 보류 — 데이터가 쌓이는
2~3주 뒤 재시도 권고.

**디자이너(3년차).** 이번 주 UI 변경 없음(데이터·README·리포트만) — 신규 리스크 없음.
`contentUsage`가 사용 저조 콘텐츠의 진입 동선/설명 부족을 점검하는 용도로 설계됐으나
표본 부족으로 이번 주는 점검 보류. 관찰(제안 아님): C-actor 노출로 카테고리 탭이
4개(K/J/US/C)로 늘었으므로, 다음 UI 변경(P2 Phase 2 등) 착수 시 카테고리+소속사 버튼
합산 가로 공간(모바일 390px)을 함께 재확인할 것.

**팀장(8년차) 종합·판정.** 이번 주는 **안정적 확장 + 의사결정 트리거 도달 주**로
판정한다. 정합성 100%, 오류 백로그 0, 추가 백로그 -50%(고유 그룹 -46%) 추세 지속,
C-actor 신규 노출로 비K 카테고리 확장 로드맵(J→US→C) 사실상 완료 — 전부 정상 범주.
**판단 포인트 1(신규):** P2(소속사 세분화) `promotable`이 전주 2개 → 이번 주 4개로
SNAPSHOT에 명시된 재문의 트리거(4개 이상)를 충족했다 — **Phase 2(`renderAgencyButtons`
상단 버튼 승격) 착수 여부를 사용자에게 묻는다**(§4). **판단 포인트 2(관찰):**
usage 계측 정체는 아직 이상 여부를 판단할 근거 부족 — 액션 없이 관찰만 지속.
그 외 액션 아이템 없음.

## 4. 개선 제안 (승인 대기)
- **P2(소속사 세분화) — Phase 2 착수 재문의 트리거 도달.** `promotable`이 4개(스타쉽·큐브·
  FNC·MODHAUS)로 전주 대비 2개 늘어 SNAPSHOT에 명시된 기준을 충족했다. Phase 1(데이터)은
  계속 진행(커버리지 79%), Phase 2는 여전히 **미착수·승인 대기**. 상세 스펙·결정 필요
  선택지는 [`../ops/PROPOSALS.md`](../ops/PROPOSALS.md) P2 참고(이번 리뷰에서 진행
  현황만 갱신, 신규 스펙 변경 없음).
- 새 제안 없음 — 이번 주는 기존 P2 트리거 도달 알림과 usage 파이프라인 관찰 기록만.

## 5. 신규 데이터·기능 아이디어
- C-actor 신규 노출(3번째 비K 카테고리)도 US-actor 때와 동일하게 "새 카테고리 열림" 토스트
  후보(전주 리뷰에서 제안한 아이디어와 동일 패턴) — 코드 변경 필요, 승인 전 미착수.
- usage 표본이 쌓이면(예: 2주 뒤 `daysCovered` ≥ 7) `contentUsage.ranking`을 이용해 저조
  콘텐츠(현재는 전부 동률이라 판단 불가)의 진입 동선 개선을 디자이너 렌즈로 재검토 —
  지금은 표본 부족으로 아이디어 단계.

## 6. 다음 주 액션
1. 백로그 소진 지속(요일 고정 스케줄 유지, 현재 추세 정상).
2. **P2 Phase 2 착수 여부 사용자 승인 대기**(promotable 4개 도달로 재문의 트리거 발동).
3. usage 계측 `updatedAt` 모니터링 — 다음 리뷰(2026-08-24)까지도 08-14에 멈춰 있으면
   팀장 판단으로 워커 점검 필요 여부 재검토.
4. 확증된 데이터 오류가 없는 한 다음 정기 리뷰(2026-08-24)까지 특이 대응 불필요.

## 부록: Notion 미러링
**노션 미러링 생략(커넥터 없음)** — 이번 세션에 Notion MCP 툴이 연결되지 않아 시도하지
않음(실패 아님, best-effort 규칙). 저장소 `docs/reports/`가 정본이다.
