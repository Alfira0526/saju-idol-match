# 주간 운영 리뷰 — 2026-08-03

## 1. 지표 요약
- **총원 775명 · 그룹 124개**(전주 651명·111그룹 대비 **+124명·+13그룹**) · 정합성 **OK**
  (`statsTotalMatches=true`, 중복 0, `node tools/build.js --check` → errors/computeMismatch/
  anchorFails/duplicates 모두 빈 배열)
- **카테고리 커버리지**: **K-idol(733) + J-idol(30) 신규 노출** — 노기자카46 30명 확장(8/2
  커밋 `9ce1366`, P1 제안 실행 완료)으로 `CAT_MIN=30` 문턱을 넘어 **2번째 카테고리 탭이 UI에
  자동 노출**되기 시작(코드 변경 없음, 데이터만). US-actor(4)·C-actor(3)·Etc(5)는 여전히
  문턱 미달로 비노출.
- **소속사 분포**: 기타 466(60.1%) · SM 80 · HYBE 73 · JYP 74 · YG 40 · 노기자카46 30(신규) ·
  배우 5 · 미국배우 4 · 중국배우 3. `기타` 세분화(subAgency) Phase 1 진행 — **238명(51%)
  라벨 부여 완료**(전주 0%). `AGENCY_MIN=15` 이상 라벨 5개(MODHAUS 51·스타쉽 32·FNC 21·
  KQ 18·울림 18) 확보.
- **제보 백로그**: 추가 대기 **454명**(전주 1,012명 대비 **-55%**, 신설 지표 기준 고유 그룹
  319개·신규 그룹 312개) / 오류 대기 **0**(전주 0 유지, 91건 전량 `inbox-done.json` 처리
  완료 확인) / 처리 누적 1,247(전주 257)
- **i18n**: **100%**(14/14 청크 — P1 영어 정적·JA 일본어 정적·P2 동적 결과 텍스트 전 항목
  완료). 전주 "13%(2/15)"로 보고됐던 값은 **집계 스크립트 버그**(아래 §2) 때문에 부정확했음 —
  실제로는 대부분 이미 7/30에 완료돼 있었고, 이번 리뷰에서 버그를 고쳐 정확한 값을 처음
  확인했다. 남은 확장은 미착수 **P3(중국어 ZH)** 뿐(§4 P5).
- **flushCadence**: 정상(최근 48h cron-delay 0건). 8/3 새벽 발견된 거짓 경보(quiet gap을
  cron-delay로 오판)는 이미 커밋 `affd68c`로 수정·확인됨(결정 로그 8/3 항목).

## 2. 오류 처리 내역
- `tools/error-inbox.json` 91건 전량 `tools/inbox-done.json`과 대조 → **전량 이미 처리
  완료**(신규 미처리 오류 0건). `idols.json` 확증 오류 수정: **0건**.
- WebSearch 교차검증(이상 후보 점검): 이번 주 최대 데이터 변경인 노기자카46 30명 중 최연소
  멤버 **카와바타 히나(川端晃菜, 2011-01-14)**의 생년이 이상 후보로 보여(2026년 기준 만 15세)
  교차검증 — 아이돌 대도감·에케페디아 등 공식/위키 프로필과 일치, **"乃木坂46 결성 연도
  출생·6기 최연소"**로 확인된 정확한 데이터. 수정 불필요.
- **파이프라인/문서 버그 2건 수정**(데이터 아님 — 지표·문서 정확성 문제):
  1. `tools/ops-metrics.js`의 i18n 진행률 정규식이 `tools/i18n-plan.md` **본문 설명 문장** 속
     `` `[x]` ``/`` `[ ]` `` 인용까지 오매칭하고(허수 가산), 부분완료 표시 `[~]`는 분모에서
     아예 누락시켜 **분자·분모 모두 부정확**했음. 줄 시작 `- [ ]/[x]/[~]` 리스트 항목만
     집계하도록 수정(`[~]`는 분모 포함·완료 미포함). `node tools/ops-metrics.js --check`
     클린 통과.
  2. `tools/i18n-plan.md`: P1-6이 부분완료(`[~]`)로 남아 있었으나 실제 잔여 작업(최애 리포트
     본문·토스트)은 이미 P2 항목에서 완료돼 있어 `[x]`로 정정. P1-7은 이미 완료된 항목(하단
     "P1-7 공유 캡션·이미지")과 **중복된 미완료 줄**이라 정리(삭제).
- 오신고 정리: 없음(대상 없음).

## 3. 페르소나 리뷰
**QA(4년차)** — 정합성 100%(build --check 클린, 중복 0), 오류 백로그 0으로 완전 소진 유지.
이번 주 발견한 진짜 이슈는 데이터가 아니라 **지표 파이프라인 자체의 버그**(i18n % 오산정) —
문서 본문 인용문까지 정규식이 긁어가는 전형적인 "느슨한 매칭" 실수였다. 리스트 항목만 보게
고치고 문서 체크박스도 실제 상태에 맞게 정리했다. J-idol 신규 확장분(30명)은 WebSearch
샘플 검증 통과 — 회귀 없음. 코어플로우/모바일/i18n 회귀 항목은 이번 주 UI 코드 변경이 없어
재검증 대상 아님(무변경).

**개발자(4년차)** — 가시 커밋 이력(8/1~8/3, 50건) 중 47건이 `chore(inbox): flush`이고 실
데이터/코드 변경은 3건(J-idol 확장, 제보 처리+subAgency 세분화, flushCadence 버그 수정) —
**커밋 폭증 아님**, 워커 큐→10분 Cron flush 파이프라인 정상. flushCadence 지표 자체가 지난주
거짓 경보를 냈던 사고가 있었지만 이미 수정·검증됐고, 이번 주 재발 없음(cron-delay 0). 이번
리뷰에서 고친 i18n 집계 버그처럼, **집계 스크립트에 좁은 스코프의 정규식 취약점**이 하나 더
있을 가능성을 염두에 두고 다음 코드 변경(예: subAgency Phase 2) 때 `ops-metrics.js`의 다른
정규식 기반 파서(flushCadence git log 파싱 등)도 한 번씩 점검할 가치가 있다고 제안(관찰
포인트, 별도 작업 티켓 아님). `build.js`는 775명 규모에서도 즉시 통과, 성능 이슈 없음.

**기획자(4년차)** — J-idol(노기자카46) 노출은 "사주 특성만으로 오락적 최애 추천"이라는
최초 기획의도를 해치지 않음 — 그룹 선택 기준이 30명 문턱(SOP)이었을 뿐 신규 카테고리 자체가
재미 요소를 늘림(해외 팬 유입 경로 개방, 지난주 제안 P1의 목표 달성). i18n이 사실상 완료
단계(100%)에 도달해 KO/EN/JA 3개 언어권 사용자 모두 "동일한 재미 경험"을 받을 수 있게 된
점도 긍정적. 다음 확장 후보(ZH)는 실제 수요 신호가 없어 지금 밀어붙이기보다 K 백로그 소진과
subAgency 세분화처럼 **이미 신호가 확인된 작업에 슬롯을 계속 쓰는 편**을 권장(§4 P5).

**디자이너(3년차)** — `기타` 소속사가 전주 대비 절대 인원은 늘었지만(381→466) **세분화가
0%→51%로 빠르게 진행**돼 실질적 필터 변별력이 개선 중. `promotable` 리스트(MODHAUS 51 등
5개 라벨이 제안 임계값 15 이상)가 확보돼 상단 버튼 승격(Phase 2 UI) 논의를 시작할 실측
근거가 생겼다 — 아직 코드 변경 승인 전이므로 이번 주는 데이터만 유지, UI는 무변경(리스크 0).
이번 주 코드/UI 변경 없어 헤드리스 스모크는 재실행하지 않음(QA 체크리스트 "리포트만 변경 시
build 불필요" 조항 적용, i18n-plan/ops-metrics.js 변경은 index.html·idols.json에 영향 없음).

**팀장 종합·판정** — 이번 주는 **데이터 정합성 100%, 확증 데이터 오류 0건**으로 데이터 개입
불필요했지만, **지표 파이프라인 버그 2건**(i18n % 오산정)을 찾아 고친 게 이번 리뷰의 핵심
성과다. 지난주 제안 P1(J-idol 노출)이 계획대로 실행돼 종료됐고, P2(소속사 세분화) Phase 1도
빠르게(0→51%) 진행 중이라 **Phase 2(UI 승격) 결정을 이제 요청할 시점**이라고 판단한다(§4).
i18n은 사실상 완료 상태라 **다음 언어(ZH) 착수는 보류 권장** — 수요 신호가 없는 확장보다
검증된 백로그(K 소진 454건, subAgency 51%)에 슬롯을 계속 쓰는 것이 ROI가 높다. 아래 4·5절은
승인 대기 '제안'이며 이번 주 자동 반영분은 데이터 오류 수정이 아닌 **지표/문서 정확성
수정**뿐이다.

## 4. 개선 제안 (승인 대기)
> 상세 실행 스펙은 [`../ops/PROPOSALS.md`](../ops/PROPOSALS.md). 아래는 요약.

- **P1 · J-idol 카테고리 노출 — ✅ 완료.** 노기자카46 30명 확장으로 목표 달성, 추가 결정
  불필요.
- **P2 · `기타` 소속사 세분화 — Phase 1 51% 진행, Phase 2(UI 승격) 결정 시점 도래.**
  `AGENCY_MIN=15` 이상 라벨 5개 확보. **결정 필요:** Phase 2 착수 여부·승격 임계값·승격 UI
  방식(상단 버튼 vs 기타 하위 칩).
- **P5 · (신규) i18n P3(중국어 ZH) 착수 여부.** P1/JA/P2 100% 완료로 다음 확장 대상 결정
  필요. **팀장 권고: 수요 신호 생길 때까지 보류**, 검증된 백로그(K 소진·subAgency)에 슬롯
  유지.

## 5. 신규 데이터·기능 아이디어
- 백로그 상위(고유 그룹 기준, 일일 루틴 Task B 참고용): 마노(Vayonn, 8건) · 가원(미야오,
  6건) · 아토(네이즈, 6건) · 고강훈(드래곤포니, 5건) · 유사랑(izna, 5건). 실행은 일일 루틴의
  몫 — 여기선 정보만 남김.

## 6. 다음 주 액션
- Phase 2(소속사 승격 UI) 승인 여부 확인 — 승인 시 `index.html` `renderAgencyButtons` 구현
  착수(공수 0.5~1일).
- i18n P3(ZH) 착수 여부 승인 확인(§4 P5, 팀장은 보류 권장).
- 추가 대기 454건 감소 추세(전주 대비 -55%) 지속 여부 관찰.
- 이번에 고친 `ops-metrics.js` 정규식 버그 유사 패턴이 다른 지표(flushCadence git log 파싱
  등)에도 있는지 다음 코드 변경 시 점검(개발자 관찰 포인트).

## 7. 사용자 결정 반영 (2026-08-03 후속)
같은 날 사용자가 §4 제안 전체에 응답, 아래와 같이 처리했다(상세는
[`../ops/PROPOSALS.md`](../ops/PROPOSALS.md) 각 항목 · [`../ops/SNAPSHOT.md`](../ops/SNAPSHOT.md)
결정 로그).

- **P1**: 기존 (b) 방식 재확인 — 이미 완료된 항목이라 추가 조치 없음.
- **P2**: "데이터만 / 임계값 30 / 상단 버튼 승격" — Phase 2 스펙(임계값·UI 방식)만 확정하고
  **코드 착수는 보류**. `tools/ops-metrics.js`의 `AGENCY_MIN`을 15→30으로 반영, 재계산 결과
  30↑ 라벨은 스타쉽엔터테인먼트(32)·MODHAUS(51) 2개로 좁혀짐(제안값 15 기준 5개에서 감소).
- **P3(flushCadence)**: 권장안(3A만) 승인 확정, 3B 불필요.
- **P4(수익화)**: "좌우 배너삽입 정도로 약한 ad 삽입" 방향 제시 — 광고 네트워크 계정·개인정보
  처리방침·GitHub Pages 상업이용 약관이 아직 없어 **이번엔 스펙만 정리, 코드 미착수**(계정
  정보 없이는 실제 광고 스크립트를 넣을 수 없음). 결정 필요 항목 3가지를 PROPOSALS.md P4에
  남김.
- **P5(i18n 중국어)**: "중국어권 사용자 다수 있음, 진행 요청" — 팀장의 보류 권고를 뒤집고
  승인, **같은 세션에서 즉시 구현**. `index.html`에 4번째 언어(ZH, 간체)를 JA와 동일한
  `TR_ZH` 오버레이 패턴으로 전체 반영(정적 UI + 동적 결과 텍스트 + 공유 이미지). 검증:
  `build.js --check`·`ops-metrics.js --check` 클린, 헤드리스 스모크(KO/EN/JA/ZH 4언어 계산·
  시각·성별 토글, JS 에러 0), 모바일 390px zh 오버플로 없음, 공유 이미지 캔버스 zh 렌더 확인.
  i18n 진행률 100%(15/15) 유지.
