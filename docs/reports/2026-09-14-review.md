# 주간 운영 리뷰 — 2026-09-14

## 1. 지표 요약
- **총원 1,623명**(전주 1,573 대비 **+50**) · **그룹 316**(+6) · 정합성 100%
  (`build.js --check`·`ops-metrics.js --check` 클린, `stats.json.total`==`idols.json` 수,
  `name|group` 중복 0).
- **카테고리 노출 6종으로 확대(신규: C-idol)**: K-idol(1,352) · J-idol(122) · C-actor(38) ·
  K-actor(36) · **C-idol(32, 신규)** · US-actor(31). Etc(12, 전주 22)는 여전히 `CAT_MIN=30`
  미달로 숨김. 이번 주 Task D가 신규 카테고리 **C-idol**(중국 아이돌, R1SE·THE9 32명)을
  개설해 문턱을 넘겨 **UI에 자동 노출**(코드 변경 없음, 데이터만).
- **소속사 분포**: 기타 1,062 · SM 86 · HYBE 92 · JYP 79 · YG 40 등. `기타` 중 **1,014명
  (96%, 전주 91%) subAgency 부여** — 라벨링이 계속 모수 증가를 앞지르며 반등 지속.
  `AGENCY_MIN=30` `promotable`은 전주와 동일 **7개**(신규 진입 없음, 정체): 스타쉽 60 ·
  MODHAUS 60(+4) · FNC 41 · 웨이크원 33 · 스윙 31 · 큐브 30 · 아이에스티 30 — **Phase 2
  착수 여부 사용자 응답 대기 5주째**([`PROPOSALS.md`](../ops/PROPOSALS.md) P2).
- **제보 백로그**: 추가 대기 **3**(고유 그룹 3, 전주 7 대비 **-57%**) · 오류 대기 **0**
  (103건 전량 처리 확인, 신규 오류 제보 없음) · 처리 완료 2,023(전주 2,006, +17).
- **i18n**: **16/16 청크(100%)** 유지. 4개 언어(KO/EN/JA/ZH) 전 화면 지원.
- **flushCadence**: 최근 7일 `chore(inbox): flush` 17건, 최대 배치 1·평균 1, medianGap
  590분, cron-delay 경보 0건 — **정상**(누적-지연 없음).
- **콘텐츠 사용 현황(`contentUsage`)**: `available=true`이나 **`updatedAt`이 전주와 완전히
  동일한 `2026-09-07T09:40:47.827Z`로 고정** — 이번 주 신규 usage 커밋이 **0건**(git log
  직접 확인, 마지막 커밋 09-07 18:40 KST 이후 오늘까지 7일간 정지). 최근 7일 순사용자 합
  **0명**(전주 2명 → 회귀). 랭킹(누적 기준, 변동 없음) — 사주궁합(match) 7 · 최애끼리 궁합
  (pair) 3 · 결과 공유(share) 3 · 최애 프로필(profile) 2 · 지옥의 삼각관계(tri) 1 · 제보
  열기(suggest) 1 · **케이팝 오타쿠 사주(otaku) 0(여전히 미계측)**. §3 개발자 관찰 참조 —
  08-24 이후 두 번째 "장기 정지 → 하루 재개 → 재정지" 사이클로 판단.

## 2. 오류 처리 내역
- `error-inbox.json` 103건 전량 `inbox-done.json` 반영 완료 확인(신규 오류 제보 0건) —
  **이번 주 WebSearch 교차검증 대상 없음, `idols.json` 데이터 오류 자동 수정 0건.**
- **툴 결함 1건 확증·직접 수정(idols.json 아님, 운영 도구 한정).** `tools/update-readme.js`의
  `CAT_KO` 한글 라벨 맵이 이번 주 신설된 `C-idol` 항목을 누락해 README STATS 블록에
  "C-idol(32)"를 영문 그대로 노출 — `index.html`의 `CAT_NAMES.C-idol.ko`("중국 아이돌")와
  동일 값으로 `CAT_KO`에 1줄 추가해 해소(재실행 확인). 2026-08-24 K-actor 누락과 **동일
  패턴의 두 번째 재발** — [`PROPOSALS.md`](../ops/PROPOSALS.md) P9(a)에 구조적 재발방지안
  (index.html CAT_NAMES 파싱 재사용)의 승인 필요성을 상향해 기록.

## 3. 페르소나 리뷰
- **QA**: 정합성 100%(`statsTotalMatches`, 중복 0), 오류 백로그 0(103건 재확인). 제보
  백로그 7→3(-57%)로 계속 안정적 소진, 회귀 없음. README STATS 라벨 결함(C-idol)은 확증
  후 즉시 수정·재검증 완료.
- **개발자**: **핵심 관찰 — usage flush가 09-06~07 하루 재개 이후 다시 완전 정지, 08-24
  이후 두 번째 "장기 정지 → 하루 재개 → 재정지" 사이클이 확인됐다.** `tools/usage.json`의
  마지막 커밋은 09-07 18:40(KST)이며 그 이후 오늘까지 7일간 신규 커밋이 0건이다.
  `contentUsage.updatedAt`도 전주와 완전히 동일한 값으로 멈춰 있어 지표가 그대로 굳어있다.
  P6(no-op 커밋 억제)·P7(otaku 계측)은 재배포 전까지는 코드가 저장소에 준비된 채 계속
  대기 상태다. 이 패턴이 반복된다는 것은 **재배포 없이는 계측 공백이 주기적으로 재발할
  것**임을 시사한다. flushCadence(제보 큐)는 17건/7일·정상, 빌드 성능 이슈 없음.
- **기획자**: 최초 기획의도(사주 특성만으로 오락적 최애 추천) 유지 확인. Task D의 C-idol
  카테고리 개방은 비K 로드맵 연장선의 순수 데이터 확장으로 기획의도와 충돌 없음.
  `contentUsage` 최근 7일 순사용자가 다시 0명으로 회귀해 **인기/저조 콘텐츠 판단은 이번
  주도 보류** — 08-19 출시한 오타쿠 사주는 4주 넘게 실사용 성과를 전혀 볼 수 없는 상태.
- **디자이너**: 표본 부재로 저조 콘텐츠의 진입 동선·설명 점검은 이번 주도 수행 불가(기획자와
  동일 사유). 신규 C-idol 카테고리는 기존 카테고리 버튼 렌더링 로직을 그대로 타므로 UI
  변경·회귀 리스크 없음(헤드리스 스모크로 확인).
- **팀장 종합·판정**: 데이터 파이프라인은 이번 주도 건강하다(정합성 100%·백로그 -57%·
  flushCadence 정상·i18n 100% 유지). **유일한 상시 리스크는 여전히 워커 재배포 건이며,
  이번 주 새로 확인된 "정지→재개→재정지" 반복 패턴은 재배포를 더 미룰 근거를 완전히
  제거한다.** **명확한 권고: P6+P7을 묶어 재배포를 우선순위 최상단으로 재승인 요청**(코드는
  이미 저장소에 준비돼 있고 변경 리스크 없음). P2(소속사 Phase 2)는 promotable 7개로
  정체됐으나 재촉하지 않고 대기 유지. README 라벨 결함(P9a)의 재발은 구조적 수정 승인을
  다시 제안한다.

## 4. 개선 제안 (승인 대기)
- **P6·P7 (재상정, 이번이 4주째)**: 워커 usage flush no-op 커밋 차단(3줄) + `otaku` 계측
  누락 해소 — **재배포 1회로 둘 다 해결**. 이번 주 새 근거: 09-06~07 하루 재개 후 다시
  완전 정지, "장기 정지→재개→재정지" 두 번째 사이클 확인. 상세:
  [`../ops/PROPOSALS.md`](../ops/PROPOSALS.md) P6·P7.
- **P2 (응답 대기 5주째)**: `기타` 소속사 Phase 2(상단 버튼 승격) 착수 여부. `promotable`
  7개로 정체(신규 진입 없음). 상세: PROPOSALS.md P2.
- **P8 (결정 대기, 3주째)**: 동일 인물 중복 노출 정책(표시 단계 제거 권고, (c)안). 이번 주도
  전수 재검사는 수행하지 않음(마지막 확인값 24건 유지). 상세: PROPOSALS.md P8.
- **P9 (a 재발 — 구조적 수정 필요, b 제안 유지)**: 카테고리 한글 라벨 이중 관리 결함이
  C-idol 개방으로 **두 번째 재발** — 이번엔 즉시 수정(1줄)했으나, `index.html` CAT_NAMES를
  파싱해 재사용하는 구조적 재발방지안의 승인 필요성이 커졌다. `currentList()` 가드 추가는
  기존 제안 그대로. 상세: PROPOSALS.md P9.

## 5. 신규 데이터·기능 아이디어
- 이번 주 신규 아이디어 없음(데이터 파이프라인 안정 운영 + 기존 제안 추적에 집중).

## 6. 다음 주 액션
- P6/P7 재배포 승인 대기 계속 추적 — 다음 주에도 재개 없이 지나가면 "정지→재개→재정지"
  주기성 자체를 데이터로 정리해 근거를 보강.
- P2 promotable 7개 정체 상태 유지 갱신, 재촉 없이 대기.
- P9(a) 구조적 재발방지안(코드 재사용) 승인 여부 회신 요청 — 재발 2회 확인으로 우선순위 상향.
- 워커 재배포가 실제 이뤄지면 `otaku` 이벤트 계측 정상화 여부와 `contentUsage` no-op 커밋
  소멸 여부, 그리고 flush 정지 사이클 재발 여부를 다음 리뷰에서 확인.
