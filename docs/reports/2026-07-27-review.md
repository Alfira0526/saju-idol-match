# 주간 운영 리뷰 — 2026-07-27

> 이번 주간 리뷰 파이프라인의 **첫 실행**이다(파이프라인 자체가 오늘 `dd27005` 커밋으로
> 구축됨). 전주 리포트가 없어 증감은 "N/A(첫 주간 리뷰, 다음 주부터 비교 가능)"로 표기한다.

## 1. 지표 요약
- **총원 651명 · 그룹 111개** · 정합성 **OK**(`statsTotalMatches=true`, 중복 0,
  `node tools/build.js --check` → errors/computeMismatch/anchorFails/duplicates 모두 빈 배열)
- **카테고리 커버리지**: K-idol 641(노출) · US-actor 4 · C-actor 3 · J-idol 2 · Etc 1
  (4개 카테고리 모두 `CAT_MIN=30` 미달로 비노출, 총 10명)
- **소속사 분포**: 기타 381(58.5%) · SM 80 · HYBE 73 · JYP 67 · YG 40
- **제보 백로그**: 추가 대기 **1,012** / 오류 대기 **0**(64건 전량 `inbox-done.json` 처리 완료
  확인) / 처리 완료 누적 257 — 전주 대비 N/A(첫 주)
- **i18n**: 2/15 청크(13%) — 전주 대비 N/A(첫 주)

## 2. 오류 처리 내역
- `tools/error-inbox.json`의 64건 오류 제보를 `tools/inbox-done.json`(257 keys)과 대조한 결과
  **전량 이미 처리 완료**(일일 루틴이 선행 처리) — 이번 주 신규 확증 오류 **0건**.
- WebSearch 교차검증(이상 후보 점검 차원, 확증 오류 목적 아님):
  - tripleS 24명 전원 — 6/2026 24人 완전체 컴백 보도([스타뉴스](https://www.starnewskorea.com/music/2026/06/15/2026061507541957422))
    와 데이터셋 24명 일치 확인.
  - Hearts2Hearts 8명(지우·카르멘·주은·유하·스텔라·에이나·이안·예온) — 공식 라인업과
    이름·인원 전원 일치.
  - → **수정 대상 없음.** `idols.json`/`inbox-done.json` 변경 없음.
- 오신고 정리: 없음(대상 없음).

## 3. 페르소나 리뷰
**QA(4년차)** — 정합성 100%(build --check 클린), 오류 백로그 0으로 완전 소진 상태. 신규
데이터(tripleS/Hearts2Hearts/CLOSE YOUR EYES) 웹 교차검증 결과 이상 없음. 이번 주 코드/UI
변경이 없어 코어플로우·모바일·i18n 회귀 항목은 재검증 대상 아님(무변경).

**개발자(4년차)** — 최근 7일 커밋 50건 중 47건이 `chore(inbox): flush N add/err`로
`tools/inbox.json`/`error-inbox.json`의 카운트 동기화(워커 큐 → 10분 Cron flush)이며
`idols.json` 실변경은 1건(17명 추가+오류 2건 반영)뿐 — **커밋 폭증 아님**, 파이프라인 정상.
다만 19:00~19:40 구간에 flush 배치가 29/31/38건으로 평소(1~6건) 대비 급증 — 데이터 변경이
아닌 카운트 동기화라 리스크는 낮으나, 유입 폭주 원인(제보 급증 vs 배치 지연 누적)은 다음 주
관찰 포인트로 남긴다. `build.js`는 651명 규모에서 즉시 통과, 성능 이슈 없음.

**기획자(4년차)** — 이번 주 변경(제보 팝업 문구 개선, CTA 버튼 스타일, 인트로 문구 리라이트)은
모두 "사주 특성만으로 오락적 최애 추천"이라는 최초 기획의도를 해치지 않음. 다만 비K 카테고리
(J-idol/C-actor/US-actor/Etc, 총 10명)가 여러 곳에 소규모로 흩어져 어느 것도 30명 문턱에
못 미치는 상태가 지속 — SOP(한 카테고리 몰아주기)대로 다음 확장 사이클엔 하나만 집중할 것을
재확인.

**디자이너(3년차)** — `기타` 소속사가 381명(58.5%)으로 정보 위계상 여전히 큰 뭉치. 필터
UX에는 문제 없으나 하위 세분화(Task A) 효과가 나오기 전까진 "기타"가 필터 옵션 중 가장 눈에
띄는 무의미 버킷으로 남는다. 이번 주 UI 문구/스타일 변경은 코드 리뷰상 특이사항 없음(별도
데이터/코드 변경이 없어 헤드리스 스모크는 이번 리뷰에서 재실행하지 않음 — QA 체크리스트
"리포트만 변경 시 build 불필요" 조항 적용).

**팀장 종합·판정** — 이번 주는 **데이터 정합성 100%, 확증 오류 0건**으로 개입 불필요.
추가 대기 1,012건은 여전히 많지만 요일 고정 스케줄(월·수·금 B/화·토 A/목·일 C)로 일일 루틴이
안정적으로 소진 중이므로 **현재 페이스 유지, 별도 조정 불필요**. 유일한 관찰 대상은 개발자가
짚은 flush 배치 급증 구간(19:00~19:40)인데, 데이터 무결성에 영향 없어 **경고 아님, 다음 주
추이만 관찰**. 아래 4·5절은 승인 대기 '제안'이며 이번 주 자동 반영분은 없다.

## 4. 개선 제안 (승인 대기)
- **[기획] 비K 카테고리 몰아주기 재확인**: J-idol(2)·C-actor(3)·US-actor(4)·Etc(1) 중
  하나를 다음 카테고리 확장 사이클에서 30명까지 집중 확장(SOP 재확인, 신규 정책 아님 —
  실행만 남음).
- **[UX] 소속사 세분화(Task A) 가속 검토**: `기타` 381명(58.5%)이 큼. 현재 요일 고정으로
  주 2회(화·토)만 배정되는데, 백로그 소진 속도 대비 세분화 진척이 더딘지 다음 주 metrics로
  추적 후 배정 비중 조정 여부 판단(**변경 아님, 관찰 후 판단 제안**).
- **[기술] Flush 배치 급증 로깅**: 19:00~19:40 같은 배치 크기 급증 구간의 원인(제보 유입
  폭주 vs Cron 지연 누적)을 구분할 수 있는 로그/카운터 추가 검토(**리스크 있는 리팩터 아님,
  관찰용 로깅 제안만**).

## 5. 신규 데이터·기능 아이디어
- 백로그 상위(count 기준) 참고 — 일일 루틴 우선순위 참고용: 최상엽(루시, 10건),
  지연(트리플에스, 7건), 소건(NEXZ, 6건), 엄성현(CORTIS, 6건), Asahi(Treasure, 6건).
  (실행은 일일 루틴 Task B의 몫 — 여기선 정보만 남김.)

## 6. 다음 주 액션
- 추가 대기/오류 대기 건수 추이 확인(이번 주가 베이스라인).
- flush 배치 크기 급증 구간 재발 여부 관찰.
- 비K 카테고리 확장 실행 여부(기획 제안 3) 사용자 승인 확인.
