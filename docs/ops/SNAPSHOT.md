# 운영 스냅샷 (SNAPSHOT)

> 주간 운영 리뷰가 **읽고 갱신**하는 살아있는 문서. 헤드라인 수치는
> `metrics-latest.json`에서 오고, 상시 이슈·결정 로그는 사람이 읽는 맥락이다.
> 상세 절차는 [`../OPS_ROUTINES.md`](../OPS_ROUTINES.md).

## 헤드라인 (as of 2026-08-31)
- **총원** 1,438명 · **그룹** 286 · 정합성 OK(stats.total==idols, 중복 0)
- **카테고리 노출 5종 유지**: K-idol(1,249) · J-idol(79) · C-actor(38) · K-actor(34) ·
  US-actor(31). 신규 노출·이탈 없이 순수 데이터 증가만(+151명). Etc(7, 전주 5)만 문턱
  미달로 숨김(개별 인물 수집, 느린 성장, 문제 아님).
- **소속사 분포**: 기타 971 · SM 86 · HYBE 85 · JYP 74 · YG 40 · 중국 배우 38 · 배우 35 ·
  미국 배우 31 · 노기자카46 30 · LAPONE 18 · avex 8 · XG 7 · ASOBISYSTEM 8 · 가수 6 ·
  페포니뮤직 1. `기타` 중 831명(**86%**, 전주 88%)에 subAgency 라벨 부여 — 절대량은
  +88명 늘었으나 `기타` 모수가 더 빨리 늘어(+121) 비율만 소폭 하락(Task A 처리량 문제 아님).
  `AGENCY_MIN=30` 승격 후보(`promotable`)가 5개 → **6개**(스타쉽엔터테인먼트 60 · MODHAUS 54 ·
  FNC엔터테인먼트 35 · 스윙엔터테인먼트 31 · 큐브엔터테인먼트 30 · **아이에스티엔터테인먼트
  30 신규**) — **Phase 2 착수 여부 사용자 응답 대기 3주째**(`PROPOSALS.md` P2).
- **제보 백로그**: 추가 대기 **26**(전주 79 대비 -67%, 고유 그룹 25·전주 67 대비 -63%,
  5주 연속 최대 감소폭) · 오류 대기 0 · 처리 완료 1,970
- **i18n**: **16/16 청크(100%)** 유지. 4개 언어(KO/EN/JA/ZH) 전 화면 지원.
- **콘텐츠 사용(`contentUsage`)**: 🔴 **08-24 이후 신규 이벤트 0건 — 계측 완전 정지.**
  `usage.json.updatedAt`이 전주 리포트와 동일 값(08-24T08:40:48Z), 최근 7일 순사용자 합
  **0명**(누적 15, 전주와 동일). 비콘 키 TTL(40h) 만료로 `flushUsage()`의 조기종료 조건이
  걸려 flush 자체가 트리거되지 않는 것으로 추정(설계된 유휴 절감 로직이 "정직하게" 작동한
  결과) — 실제 무방문/비콘 경로 이상/구버전 배포 지속 중 어느 것인지는 **재배포 후에만
  구분 가능**. 아래 상시이슈 1 참조. 상세: [`../reports/2026-08-31-review.md`](../reports/2026-08-31-review.md).

## 상시 이슈 (standing issues)
1. **🔴 배포된 워커가 구버전 — 재배포 1회 필요(2주째 최우선, 시급도 상향).**
   - **(P6) usage flush가 하루 ~144건의 무변경 커밋을 만든다.** 08-19~08-24 `usage.json`
     커밋 **718건 중 713건(99.3%)이 `updatedAt` 한 줄만 변경**(전수 diff 검사). 원인은
     `worker/suggest-worker.js:136-137`의 조기 종료가 "`u:` 키가 하나도 없을 때"뿐이고,
     `:165`가 매 틱 새 `updatedAt`을 찍어 항상 diff를 만들기 때문. **데이터 정합성 영향은 없음.**
   - **(P7) 5번째 모드 `otaku` 사용량이 전혀 수집되지 않는다.** 배포본이 08-19 이전 코드라
     `otaku` 비콘이 `:260` 화이트리스트에서 204로 버려진다. **코드 변경 없이 재배포만으로 해소.**
   - **🆕 (08-31 신규 관찰) usage flush가 08-24 17:40 이후 완전히 정지.** `usage.json`
     커밋이 지난 7일간 **0건** — 비콘 키 TTL(40h) 만료로 `u:` 키가 전부 소멸해 flush 조기종료
     조건이 상시로 걸린 것으로 추정. 실제 무방문/비콘 경로 이상/배포 문제 지속 중 어느 것인지는
     **재배포 후에만 구분 가능**(egress 차단으로 라이브 확인 불가). 콘텐츠 사용 현황(`contentUsage`)
     최근 7일 순사용자가 **9명(08-24) → 0명(08-31)**으로 악화.
   - → **P6 코드 수정(3줄) + 재배포 1회로 셋 다 해결.** 승인 대기([`PROPOSALS.md`](PROPOSALS.md) P6·P7).
2. **추가 제보 백로그.** 26건(고유 그룹 25)으로 전주 대비 -67%(고유 그룹 -63%), 5주 연속
   최대 감소폭 — 요일 고정 스케줄로 안정적 소진 중. 급증 반전 시에만 팀장 판단으로 일일 Task
   비중 조정.
3. **`기타` 소속사 세분화 Phase 2 착수 여부 — 사용자 응답 대기(3주째).**
   Phase 1(subAgency 데이터 필드) **86%** 진행(전주 88% — `기타` 모수 급증으로 비율만 소폭
   하락, 라벨링 절대량은 +88명). `promotable` 5개 → **6개**(스타쉽·MODHAUS·FNC·스윙·큐브·
   **아이에스티엔터테인먼트 신규**)로 확대돼 조건은 더 무르익었으나 재문의에 계속 응답 없음.
   **재촉하지 않고 대기 유지** — Phase 2(`index.html` `renderAgencyButtons` 상단 버튼 승격,
   공수 0.5~1일) 결정 필요([`PROPOSALS.md`](PROPOSALS.md) P2).
4. **동일 인물 중복 노출 24건(전주 23건) — 정책 결정 대기.** 같은 실존 인물이 다른 그룹명으로
   중복 수록된 항목. 신규 추가분(+151명) 대비 증가폭 미미해 구조적 패턴 악화는 아님.
   프로젝트·해체 그룹에 집중된 **구조적 부작용**이며, `build.js`의 `name|group` 중복 검사로는
   영원히 검출되지 않는다. **팀장 권고: 데이터는 유지하고 순위 산출 시 동일인 1회만 노출**
   ([`PROPOSALS.md`](PROPOSALS.md) P8).
5. **비K 카테고리 확장 — 로드맵 완료.** 노출 5종(K-idol·J-idol·C-actor·K-actor·US-actor)
   구성 변화 없이 이번 주 순수 데이터 증가(+151명). Etc(7)만 개별 인물 수집이라 30 도달까지
   느림 — 별도 슬롯 배정 전까진 정체 예상(문제 아님).
6. **수익화 — AdSense 자동 광고 활성 상태.** 2026-08-07 커밋 `7dc6c27`로 게시자 ID 연결
   완료(`AD_CLIENT="ca-pub-6294147440984199"`, `AD_SLOT="8314531876"` 하단 디스플레이 배너).
   서버 비용 증가는 0(전부 클라이언트 연산). 개인정보 고지 문구는 `AD_CLIENT` 존재 시
   자동 노출(`initAds`). 상세: [`monetization-review.md`](monetization-review.md).
   ※ 08-17 스냅샷의 "계정 대기·현재 무광고" 기술은 08-07 시점에 이미 낡은 것이라 정정함.

## 결정 로그 (decisions)
- 2026-08-31 — **[주간 리뷰] 데이터 건강 양호, 그러나 콘텐츠 사용 계측이 완전 정지.**
  정합성 100%(build.js/ops-metrics.js `--check` 클린, 중복 0, statsTotal 일치), 오류 백로그 0
  (100건 전량 처리 재확인), **`idols.json` 데이터 오류 자동 수정 0건**(신규 오류 제보 없음).
  총원 1,287→1,438(+151), 그룹 257→286(+29). 카테고리 노출 5종 구성 변화 없음(순수 데이터
  증가). subAgency 커버리지 88%→86%(모수 급증 탓, 라벨링 절대량은 +88명 증가로 정상).
  `promotable` 5→**6개**(아이에스티엔터테인먼트 신규). 백로그 79→26(-67%, 고유 그룹 67→25
  -63%) — 5주 연속 최대 감소폭. i18n 16/16(100%) 유지. 헤드리스 스모크(Playwright Chromium,
  390/1280px, 로컬 HTTP 서버) 직접 재수행 — `IDOLS.length=1438`, 계산→순위→전체 펼치기(398행)
  정상, **JS 에러 0·가로 오버플로 0**. **이번 주 핵심 발견:** `tools/usage.json`이 지난 리뷰
  (08-24 17:40 KST) 이후 **7일간 단 한 번도 커밋되지 않음**(git 로그 직접 확인) —
  `contentUsage.updatedAt`이 전주와 완전히 동일한 값으로 고정, 최근 7일 순사용자가
  9명(08-24)→**0명(08-31)**으로 악화. 원인은 비콘 키 TTL(40h) 만료로 워커
  `flushUsage()`의 조기종료 조건이 상시로 걸린 것으로 추정 — 지난주 지목한 P6/P7(구버전 배포·
  무변경 커밋)이 재배포 없이 한 주 더 지속되며 도달한 다음 단계로 판단. egress 차단으로
  라이브 확인은 불가해 재배포 전까지는 "실제 무방문"과 "계측 이상"을 구분할 수 없다.
  **P6/P7 재배포 요청을 새 근거(계측 완전 정지)와 함께 재상정.** 동일 인물 중복 후보는
  23건→24건(신규 추가분 대비 증가폭 미미, 구조적 악화 아님), P8 정책 결정 계속 대기.
  P2(소속사 Phase 2)는 3주째 무응답 — 재촉하지 않고 상태만 갱신. README STATS 블록 자동
  갱신(`node tools/update-readme.js`), 지난주 수정한 `CAT_KO` K-actor 라벨 회귀 없음 확인.
  노션 미러링은 **커넥터 미제공으로 생략**. 상세:
  [`../reports/2026-08-31-review.md`](../reports/2026-08-31-review.md).
- 2026-08-24 — **[주간 리뷰] 배포 워커 구버전 확인 — 무변경 커밋 하루 144건 + 신규 모드 미계측.**
  정합성 100%(build.js/ops-metrics.js `--check` 클린, 중복 0, statsTotal 일치), 오류 백로그 0
  (99건 전량 처리), **`idols.json` 데이터 오류 자동 수정 0건**. 총원 1,137→1,287(+150),
  그룹 237→257(+20), **K-actor(31)가 CAT_MIN 달성해 노출 카테고리 5종**(커밋 `fe52d69`,
  코드 변경 없이 데이터만). 백로그 126→79(-37%), subAgency 커버리지 79%→88%, promotable
  4→5개(스윙 31 신규). i18n 16/16(100%) 유지. **이번 주 핵심 발견 2건(동일 원인):**
  ① `usage.json` 커밋 718건 중 **713건(99.3%)이 `updatedAt`만 바뀐 no-op** — 원인은
  `worker/suggest-worker.js:136-137`의 조기 종료가 "`u:` 키 0개일 때"뿐이고 `:165`가 매 틱
  새 타임스탬프를 찍는 것(→ P6, 3줄 수정 + 재배포). ② 배포본의 `USAGE_EVENTS`가 6종이라
  08-19 출시한 **케이팝 오타쿠 사주의 `otaku` 비콘이 204로 버려져 사용량이 영구 0** —
  "저조"가 아니라 "미계측"(→ P7, **코드 변경 0, 재배포만**). **둘 다 재배포 1회로 해소되므로
  묶어서 승인 요청.** QA가 찾은 `window._saju` 미가드 3곳(`currentList` 계열)은 계산 전
  `#moreBtn`이 숨김이라 **사용자 도달 불가 확인 → 고치지 않고 P9로 보류**. 동일 인물 중복
  23건은 사실 확증(김세정=아이오아이=구구단)했으나 프로젝트 그룹 데이터 삭제는 정책 사안이라
  **자동 수정 안 함, P8로 결정 요청**(팀장 권고: 표시 단계에서만 중복 제거). 확증 defect
  1건만 자동 수정 — `tools/update-readme.js`의 `CAT_KO`에 `K-actor` 한글 라벨 누락으로 README가
  "K-actor(31)"를 영문 노출하던 것을 "한국 배우(31)"로 정정(라벨 단일 소스화는 P9로 제안).
  헤드리스 스모크(390/1280px, 5개 모드, 4개 언어) **JS 에러 0·가로 오버플로 0**. README STATS
  블록 자동 갱신. 노션 미러링은 **커넥터 미제공으로 생략**. 상세:
  [`../reports/2026-08-24-review.md`](../reports/2026-08-24-review.md).
- 2026-08-17 — **[주간 리뷰] C-actor 세 번째 비K 카테고리 노출 + P2 재문의 트리거 도달.**
  정합성 100%(build.js/ops-metrics.js `--check` 클린), 오류 백로그 0(96건 전량 처리 확인,
  이번 주 신규 오류 제보 없어 WebSearch 교차검증 대상 없음), `idols.json` 데이터 오류 자동
  수정 0건. 총원 968→1,137(+169), 그룹 177→237(+60) — Task D 커밋 `4971845`(M.O.N.T/
  MBLAQ/ALLDAY PROJECT/OMEGA X gap-fill + C-actor 37명 확장)로 **C-actor가 CAT_MIN(30)
  달성, J-idol·US-actor에 이은 세 번째 비K 카테고리 노출**(코드 변경 없음, 데이터만).
  subAgency 커버리지 70%→79%, `promotable`이 2개→**4개**(스타쉽·큐브·FNC·MODHAUS)로 늘어
  지난 리뷰가 예고한 재문의 조건 충족 — **P2 Phase 2 착수 여부 사용자 승인 대기**로 전환
  (PROPOSALS.md P2 갱신). 콘텐츠 사용 계측(`contentUsage`)은 08-13 하루치(총 6건)에서
  3일째 정체 확인 — 표본 부족으로 인기/저조 판단 보류, 개발자 관찰 항목으로 등록(액션 없음).
  README STATS 블록 자동 갱신(`node tools/update-readme.js`). 상세:
  [`../reports/2026-08-17-review.md`](../reports/2026-08-17-review.md).
- 2026-08-12 — **[사용자 승인] 콘텐츠 항목별 접속(사용) 정도 계측 도입(워커 익명 비콘).**
  기존엔 사용 분석이 전무(ops-metrics도 "static app — no usage backend")해 어떤 콘텐츠가
  쓰이는지 리포트에서 볼 수 없었음. egress 차단 환경에서 유일하게 되는 방식(제보 파이프라인과
  동일)으로 구현: 앱이 각 메뉴(match/pair/profile/tri/share/suggest) 진입 시 **기능 이름만**
  익명 비콘 → 워커 `POST /beacon`이 KV에 **하루 1인 1이벤트**(`u:{date}:{event}:{ipHash}`, TTL 40h)
  로 기록 → cron이 `tools/usage.json`(날짜별 순사용자수)으로 미러링 → `ops-metrics.js`가
  `contentUsage`(항목별 최근 7일·점유율·순위) 산출 → 주간 리포트 §1에 반영. 개인정보·생년월일
  미전송, 클라이언트 하루 1회 제한 + DNT 존중, 개인정보 고지 문구 추가. **활성화 조건: 사용자가
  워커 재배포**(신규 `/beacon`·`flushUsage` 반영). 미배포/무데이터 시 `contentUsage.available=false`.
  KV 쓰기 예산: 순사용자 1명=최대 6쓰기/일 → 무료 한도(1,000/일)로 ~160 DAU까지. build/ops-metrics
  `--check` 클린, 워커 `node --check` OK, 헤드리스 스모크(비콘 이벤트·하루중복제거·DNT·페이로드
  `{"e":...}`·JS 에러 0) 통과 후 커밋.
- 2026-08-10 — **[주간 리뷰] 안정적 확장 주 — 액션 아이템 없음.** 정합성 100%(build.js/
  ops-metrics.js `--check` 클린), 오류 백로그 0(95건 전량 처리 확인, ILLIT 민주 생일 재검증
  1건 WebSearch 교차검증 통과), `idols.json` 데이터 오류 자동 수정 0건. 총원 775→968
  (+193), US-actor가 8/9 Task D로 CAT_MIN(30) 달성해 J-idol에 이은 두 번째 비K 카테고리
  UI 노출(코드 변경 없음). subAgency 커버리지 51%→70%이나 `promotable`은 2개로 정체 —
  Phase 2 착수는 계속 보류, promotable 4개 이상 시 재문의 권고. 8/7 배포된 신규 기능
  (삼각관계 생년월일 직접입력, 최애끼리 로맨틱 프레이밍 삭제)을 리뷰에서 직접 헤드리스
  스모크(모바일 390px)로 재확인 — JS 에러 0·오버플로 없음. 상세: [`../reports/2026-08-10-review.md`](../reports/2026-08-10-review.md).
- 2026-08-03 — **[사용자 승인 5건 반영]** 주간 리뷰 제안에 대한 사용자 결정 처리.
  **P1**: 기존 (b) 방식 재확인(이미 완료, 조치 없음). **P2**: Phase 1(데이터)만 계속,
  Phase 2 스펙 확정(`AGENCY_MIN=30`·상단 버튼 승격)만 기록하고 코드는 미착수 —
  `tools/ops-metrics.js`의 `AGENCY_MIN` 15→30 반영, `promotable` 재계산(2개: 스타쉽·MODHAUS).
  **P3**(flushCadence): 3A 권장안 승인 확정, 3B 불필요. **P4**(수익화): "좌우 배너 · 약한
  광고"로 방향 전환 제시했으나 광고 네트워크 계정·정책 페이지 부재로 이번엔 스펙만 정리하고
  코드 미착수(안전 규칙 — 계정 정보 없이 실제 광고 스크립트 삽입 불가). **P5**(i18n
  중국어): "수요 있음, 진행 요청" 승인 → **같은 세션에서 즉시 구현**. `index.html`에 JA와
  동일한 `TR_ZH` 오버레이 패턴으로 4번째 언어(ZH, 간체) 전체 반영 — 정적 UI·동적 결과
  텍스트(십신·지지관계·명리 용어·최애 리포트·공유 캡션/이미지) 전부. `build.js --check`·
  `ops-metrics.js --check` 클린, 헤드리스 스모크 KO/EN/JA/ZH 4언어 JS 에러 0, 모바일 390px
  오버플로 없음 확인 후 커밋. i18n 진행률 100%(15/15) 유지. 상세: PROPOSALS.md 각 항목.
- 2026-08-03 — **[주간 리뷰] 지표 파이프라인 버그 2건 수정.** `tools/ops-metrics.js`의 i18n
  진행률 정규식이 `tools/i18n-plan.md` 본문 인용 문장 속 `[x]`/`[ ]`까지 오매칭하고 부분완료
  `[~]`는 분모에서 누락 → 줄 시작 리스트 항목만 집계하도록 수정(`[~]`는 분모 포함·완료 미포함).
  `i18n-plan.md`의 P1-6(부분완료로 남아있었으나 실제 완료)·P1-7(중복 미완료 줄) 체크박스도
  실제 상태로 정정. 결과: i18n 진행률이 정확히 **100%(14/14)**로 확인(이전 "13%"·"88%" 모두
  집계 버그로 부정확한 값). `idols.json` 데이터 오류는 이번 주 0건(91건 오류 제보 전량 기처리
  확인). J-idol(노기자카46) 30명 확장분 WebSearch 교차검증(최연소 멤버 생년) 통과.
  P1(J-idol 노출) 완료 처리, P2(소속사 세분화) Phase 2 결정 요청, 신규 P5(i18n ZH 착수 여부)
  제안. 상세: [`../reports/2026-08-03-review.md`](../reports/2026-08-03-review.md).
- 2026-08-03 — **[오탐 수정] 헬스체크 flushCadence [주의] 거짓 경보.** 8/3 새벽 헬스체크가
  medianGap 30·다수 cron-delay로 [주의] 발령 → 5인 페르소나 검토 결과 **거짓 경보 확정**:
  실제 flush 배치가 거의 `1`(gap 커도 batch 작음=조용한 시간대), median 10분·10분 틱 규칙적
  → **워커 Cron 정상**. 원인은 지표 분류기(gap만 보고 cron-delay 판정). 수정: `flushCadence`가
  **gap>20m AND batch≥10(누적)** 일 때만 cron-delay로 보고, 경보는 **최근 48h 반복시**만
  (과거 초기-세팅기 이벤트 제외). quiet/inflow-surge/cron-delay 구분 + `assessment` 필드 추가.
  워커는 무수정. (cronDelay 이벤트는 전부 7/28~7/31, 최근 48h 0건 → 현재 정상.)
- 2026-07-27 — 일일 태스크 선택을 `(day-of-year) mod 3` 무작위 → **요일 고정**(월·수·금 B /
  화·토 A / 목·일 C)으로 전환. (예측 가능성)
- 2026-07-27 — 제보 인테이크를 워커 per-commit → **KV 큐 + 10분 Cron 배치**로 전환(리소스 절약).
- 2026-07-27 — 첫 진입 "새 그룹" 전체화면 팝업 → **상단 토스트**. 필터(검색·소속사·그룹·유형)
  **접이식**으로 정리.
- 2026-07-27 — **주간 운영 리뷰 첫 실행**(`docs/reports/2026-07-27-review.md`). 정합성 100%·
  확증 오류 0건 → 자동 수정 없음. 개선 제안 3건은 [`PROPOSALS.md`](PROPOSALS.md)로 구체화.
- 2026-07-27 — **제안 3건 결정·반영**: (P1) 일요일 **Task D 비K 카테고리 확장 전용 슬롯**
  신설(우선 J-idol). (P2) `기타` 세분화 **Phase 1**(subAgency 필드) 기반 구축 — build.js·
  ops-metrics 지원, Task A가 점진 부여, UI 불변. (P3) **flushCadence 계측 완료**(3A) —
  cron-delay/inflow-surge 자동 구분. 요일 고정표: 월·수·금 B / 화·토 A / 목 C / 일 D.
- 2026-07-27 — **build.js `--check` 가드 강화**: 카테고리 라벨(CAT_ORDER·CAT_NAMES)·subAgency
  배치 무결성 → 3개 루틴 공통 자동 차단. 헬스체크에 ops-metrics 무결성(d)·flushCadence
  Cron 생존 관찰(5) 추가.
- 2026-07-27 — **P4 수익화(광고) 검토 → 보류.** 5인 페르소나 토의 완료(`monetization-review.md`).
  실비용 ~0라 침습적 광고 미도입. 재검토 트리거: Worker 무료 티어 한계 접근·트래픽 급증.
- 2026-07-30 — **[버그 수정] 요일 로테이션 미작동 발견·수정.** 점검 결과 Task A(subAgency
  0/381)·C(i18n 13% 그대로)·D(J-idol 2 그대로)가 **한 번도 실행 안 됨** — step 4가 "제보 없을
  때만" 조건이라 추가 제보 백로그(1,012→1,316, 증가 중)에 막혀 매일 제보 처리(B성격)만 반복.
  → step 4 게이트 제거(제보 유무 무관 매일 요일 태스크 필수) + step 3 추가제보 1회 상한(그룹
  2팀/~15명). tools/README.md·일일 트리거 반영.
- 2026-07-30 — **[처리량 조정] 백로그 실측 후 정책 수정.** 개인 제보 1,316은 **로마자⇄한글
  변형·오타로 과대**임을 확인 — 실질은 **고유 그룹 479개**(그나마 세븐틴=SEVENTEEN, 에이티즈=
  ATEEZ 등 변형·오타·노이즈 다수 포함). 조정: ① `ops-metrics` backlog.addPendingGroups 신설
  (distinctGroups/newGroups/knownGroups — 정직한 지표) ② step 3 상한 3팀·B슬롯 5팀으로 상향
  ③ **변형·중복·노이즈 즉시 소각(재추가 없이 inbox-done 정리)** 규칙 신설 — 백로그 최속 감소책.
