# 운영 스냅샷 (SNAPSHOT)

> 주간 운영 리뷰가 **읽고 갱신**하는 살아있는 문서. 헤드라인 수치는
> `metrics-latest.json`에서 오고, 상시 이슈·결정 로그는 사람이 읽는 맥락이다.
> 상세 절차는 [`../OPS_ROUTINES.md`](../OPS_ROUTINES.md).

## 헤드라인 (as of 2026-08-03)
- **총원** 775명 · **그룹** 124 · 정합성 OK(stats.total==idols, 중복 0)
- **카테고리 노출**: **K-idol(733) + J-idol(30) 노출**(8/2 노기자카46 확장으로 신규 진입).
  C-actor(3)·US-actor(4)·Etc(5)는 여전히 30 문턱 미달로 숨김.
- **소속사 분포**: 기타 466 · SM 80 · HYBE 73 · JYP 74 · YG 40 · 노기자카46 30. `기타` 중
  238명(51%)에 subAgency 라벨 부여 완료(Phase 1 진행 중).
- **제보 백로그**: 추가 대기 **454**(전주 1,012 대비 -55%, 고유 그룹 319) · 오류 대기 0 ·
  처리 완료 1,247
- **i18n**: **14/14 청크(100%)** — P1(영어 정적)·JA·P2(동적) 전 완료. 남은 것은 미착수
  P3(ZH). (전주 "13%" 표기는 집계 스크립트 버그였음 — 2026-08-03 수정.)

## 상시 이슈 (standing issues)
1. **추가 제보 백로그.** 454건(고유 그룹 319)으로 전주 대비 -55% 감소 추세 — 요일 고정
   스케줄로 안정적 소진 중. 급증 반전 시에만 팀장 판단으로 일일 Task 비중 조정.
2. **`기타` 소속사 세분화 Phase 2 결정 대기.** Phase 1(subAgency 데이터 필드) 51% 진행,
   `AGENCY_MIN=15` 이상 라벨 5개 확보(MODHAUS 51·스타쉽 32·FNC 21·KQ 18·울림 18) — UI
   승격(Phase 2) 여부·임계값·방식 사용자 결정 필요([`PROPOSALS.md`](PROPOSALS.md) P2).
3. **비K 카테고리 확장 다음 대상 없음.** J-idol 달성으로 완료. C-actor/US-actor/Etc는
   개별 인물 수집이라 30 도달까지 느림 — 별도 슬롯 배정 전까진 정체 예상(문제 아님).
4. **i18n P3(ZH) 착수 여부 결정 대기.** P1/JA/P2 100% 완료. 팀장은 수요 신호 없어 보류
   권장([`PROPOSALS.md`](PROPOSALS.md) P5).

## 결정 로그 (decisions)
- 2026-08-03 — **[주간 리뷰] 지표 파이프라인 버그 2건 수정.** `tools/ops-metrics.js`의 i18n
  진행률 정규식이 `tools/i18n-plan.md` 본문 인용 문장 속 `[x]`/`[ ]`까지 오매칭하고 부분완료
  `[~]`는 분모에서 누락 → 줄 시작 리스트 항목만 집계하도록 수정(`[~]`는 분모 포함·완료 미포함).
  `i18n-plan.md`의 P1-6(부분완료로 남아있었으나 실제 완료)·P1-7(중복 미완료 줄) 체크박스도
  실제 상태로 정정. 결과: i18n 진행률이 정확히 **100%(14/14)**로 확인(이전 "13%"·"88%" 모두
  집계 버그로 부정확한 값). `idols.json` 데이터 오류는 이번 주 0건(91건 오류 제보 전량 기처리
  확인). J-idol(노기자카46) 30명 확장분 WebSearch 교차검증(최연소 멤버 생년) 통과.
  P1(J-idol 노출) 완료 처리, P2(소속사 세분화) Phase 2 결정 요청, 신규 P5(i18n ZH 착수 여부)
  제안. 상세: [`../reports/2026-08-03-review.md`](../reports/2026-08-03-review.md).
- 2026-08-03 — **[오탐 수정] 헬스체크 flushCadence [주의] 거짓 경보.** 8/3 새벽 헬스체크가
  medianGap 30·다수 cron-delay로 [주의] 발령 → 5인 페르소나 검토 결과 **거짓 경보 확정**:
  실제 flush 배치가 거의 `1`(gap 커도 batch 작음=조용한 시간대), median 10분·10분 틱 규칙적
  → **워커 Cron 정상**. 원인은 지표 분류기(gap만 보고 cron-delay 판정). 수정: `flushCadence`가
  **gap>20m AND batch≥10(누적)** 일 때만 cron-delay로 보고, 경보는 **최근 48h 반복시**만
  (과거 초기-세팅기 이벤트 제외). quiet/inflow-surge/cron-delay 구분 + `assessment` 필드 추가.
  워커는 무수정. (cronDelay 이벤트는 전부 7/28~7/31, 최근 48h 0건 → 현재 정상.)
- 2026-07-27 — 일일 태스크 선택을 `(day-of-year) mod 3` 무작위 → **요일 고정**(월·수·금 B /
  화·토 A / 목·일 C)으로 전환. (예측 가능성)
- 2026-07-27 — 제보 인테이크를 워커 per-commit → **KV 큐 + 10분 Cron 배치**로 전환(리소스 절약).
- 2026-07-27 — 첫 진입 "새 그룹" 전체화면 팝업 → **상단 토스트**. 필터(검색·소속사·그룹·유형)
  **접이식**으로 정리.
- 2026-07-27 — **주간 운영 리뷰 첫 실행**(`docs/reports/2026-07-27-review.md`). 정합성 100%·
  확증 오류 0건 → 자동 수정 없음. 개선 제안 3건은 [`PROPOSALS.md`](PROPOSALS.md)로 구체화.
- 2026-07-27 — **제안 3건 결정·반영**: (P1) 일요일 **Task D 비K 카테고리 확장 전용 슬롯**
  신설(우선 J-idol). (P2) `기타` 세분화 **Phase 1**(subAgency 필드) 기반 구축 — build.js·
  ops-metrics 지원, Task A가 점진 부여, UI 불변. (P3) **flushCadence 계측 완료**(3A) —
  cron-delay/inflow-surge 자동 구분. 요일 고정표: 월·수·금 B / 화·토 A / 목 C / 일 D.
- 2026-07-27 — **build.js `--check` 가드 강화**: 카테고리 라벨(CAT_ORDER·CAT_NAMES)·subAgency
  배치 무결성 → 3개 루틴 공통 자동 차단. 헬스체크에 ops-metrics 무결성(d)·flushCadence
  Cron 생존 관찰(5) 추가.
- 2026-07-27 — **P4 수익화(광고) 검토 → 보류.** 5인 페르소나 토의 완료(`monetization-review.md`).
  실비용 ~0라 침습적 광고 미도입. 재검토 트리거: Worker 무료 티어 한계 접근·트래픽 급증.
- 2026-07-30 — **[버그 수정] 요일 로테이션 미작동 발견·수정.** 점검 결과 Task A(subAgency
  0/381)·C(i18n 13% 그대로)·D(J-idol 2 그대로)가 **한 번도 실행 안 됨** — step 4가 "제보 없을
  때만" 조건이라 추가 제보 백로그(1,012→1,316, 증가 중)에 막혀 매일 제보 처리(B성격)만 반복.
  → step 4 게이트 제거(제보 유무 무관 매일 요일 태스크 필수) + step 3 추가제보 1회 상한(그룹
  2팀/~15명). tools/README.md·일일 트리거 반영.
- 2026-07-30 — **[처리량 조정] 백로그 실측 후 정책 수정.** 개인 제보 1,316은 **로마자⇄한글
  변형·오타로 과대**임을 확인 — 실질은 **고유 그룹 479개**(그나마 세븐틴=SEVENTEEN, 에이티즈=
  ATEEZ 등 변형·오타·노이즈 다수 포함). 조정: ① `ops-metrics` backlog.addPendingGroups 신설
  (distinctGroups/newGroups/knownGroups — 정직한 지표) ② step 3 상한 3팀·B슬롯 5팀으로 상향
  ③ **변형·중복·노이즈 즉시 소각(재추가 없이 inbox-done 정리)** 규칙 신설 — 백로그 최속 감소책.
